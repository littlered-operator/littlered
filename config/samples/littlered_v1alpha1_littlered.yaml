apiVersion: chuck-chuck-chuck.net/v1alpha1
kind: LittleRed
metadata:
  labels:
    app.kubernetes.io/name: littlered-operator
    app.kubernetes.io/managed-by: kustomize
  name: my-cache
spec:
  # Minimal standalone example - uses all defaults:
  # - Image: docker.io/valkey/valkey:8.0
  # - Resources: 250m CPU, 256Mi memory (Guaranteed QoS)
  # - Metrics exporter: enabled
  # - No persistence (in-memory only)
  # - No auth, no TLS
  {}
