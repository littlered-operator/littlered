apiVersion: chuck-chuck-chuck.net/v1alpha1
kind: LittleRed
metadata:
  name: store-auth
spec:
  # Enable password authentication
  auth:
    enabled: true
    existingSecret: store-password  # Secret must have 'password' key

---
# Create the password secret first:
# kubectl create secret generic store-password --from-literal=password=mysecretpassword
