{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "littlered-operator.fullname" . }}-metrics
  labels:
    {{- include "littlered-operator.labels" . | nindent 4 }}
    control-plane: controller-manager
spec:
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      protocol: TCP
      targetPort: {{ .Values.metrics.port }}
  selector:
    {{- include "littlered-operator.selectorLabels" . | nindent 4 }}
    control-plane: controller-manager
{{- end }}
